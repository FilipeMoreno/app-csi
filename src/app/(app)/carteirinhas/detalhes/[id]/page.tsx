'use client'

import { useToast } from '@/components/ui/use-toast'

export default function CarteirinhasScanner() {
  const { toast } = useToast()

  return <div className="flex flex-wrap gap-8"></div>
}
